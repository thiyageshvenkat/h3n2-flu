env {
    KEY_MODELLER = 'PASTE_KEY_HERE_LATER' 
}

docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}

process {
    withName: BUILD_HOMOLOGY_MODEL {
        container = 'vulcan:latest'
    }
}